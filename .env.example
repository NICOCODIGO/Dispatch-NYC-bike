# Copy to .env and fill in. Vite inlines VITE_* into the client bundle, so a
# public (pk.) Mapbox token is the only kind that belongs here — and it must
# carry URL restrictions in the Mapbox dashboard, which is the actual control.
# Without a token the map falls back to the schematic view and says so.
VITE_MAPBOX_TOKEN=
